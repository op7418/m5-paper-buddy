.pio/
.vscode/
.DS_Store
# Upstream ignored data/ entirely for personal GIF pet packs. The Paper
# build ships a required CJK font file at data/cjk.ttf, so keep data/
# tracked and only ignore scratch subfolders.
data/characters/
data/.tmp/
